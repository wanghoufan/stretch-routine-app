# PLAN｜TASK-018 动作库动态部位筛选

- DEV_BASELINE：PRODUCT_PLAN_V1.2
- CHANGE_REQUEST：B（用户明确指定的 Phase2 设计复核；不触发 Change C，不修改 Plan Version / DEV_BASELINE）

## Product Goal

让用户在动作库中按当前关注的场景快速筛选部位，同时保留全局名称搜索和多场景独立展开能力；移除动作库顶部难度筛选，但不改动作难度数据、编辑能力和流程模板页。

## Current Stage

- Stage ID：stretch-app-v11-ambient / TASK-018-library-dynamic-filter
- Goal：完成 B-4 口径消歧、最小实现、自动化测试、真机验收和监督复检。
- PROJECT_PHASE：DEVELOP
- 当前状态（2026-09-19 收工核对）：**已全链完成并放行**——HD-1=A 已由用户拍板；Builder（首轮＋3 轮返工）→Code Reviewer（P0=0 / blocking P1=0，含 Round2 delta 复核）→QA 真机两轮 PASS→Supervisor 复检放行。产出在工作区未提交（commit/push 待用户指令）。本行仅同步状态，设计内容与结论未改。

## Stage P0（P0=本 Stage 非做不可、缺它不算完的事项，由 planner 初定、Task Manager 拍板）

- [x] `HD-1` 明确部位 chips 的单选/多选模型及平铺列表的重复计数语义。（2026-09-19 用户拍板=A：单选＋同屏去重，badge=当前显示唯一动作数）
- [x] Builder 完成动态部位 chips、活动场景、全局搜索组合规则及旧 UI 清理。（首轮＋3 轮返工，含 QA 真机 C-2 引发的 ⑨⑩⑪ 局部修正）
- [x] Code Reviewer 确认实现与本计划、B-4 及可访问性/testID 契约一致，P0=0、blocking P1=0。（首轮＋Round2 delta 复核均过）
- [x] QA 完成指定单测、集成测试、全量回归和 xagapro / Expo Go 真机验证；Supervisor 复检通过。（35 suites / 258 tests＋typecheck exit 0；真机两轮 PASS；Supervisor 放行）

## 代码事实复核

TM 所列现状总体准确，补充与纠正如下：

1. `ActionFilterBar.tsx` 当前确为名称搜索框加难度单选 chips（全部/低/中/高），chips 使用 `radiogroup/radio`，但 chip 高度仅 `36`，低于项目 `MIN_TOUCH_SIZE=48`。
2. `actionGroups.ts` 当前按固定顺序输出拉伸/热身/核心训练；场景判定为热身优先、核心其次、其余拉伸。只有拉伸生成二级部位组，热身和核心训练直接使用 `actions`。
3. `seeds.ts` 中三类场景动作均有多部位数据；热身含全身/肩/腿/腰腹等，核心训练含腰腹/髋臀/背等。带“热身+核心”的登山跑按现规则只归热身。
4. `bodyPartOfAction()` 当前按 `BODY_PART_GROUPS` 固定顺序取第一个规范部位；无标签或只有未知标签均归“其他”，所以每个动作当前只进入一个二级组。
5. `ActionFilters` 当前只有 `{ difficulty, query }`；难度或搜索激活后，未被用户显式覆盖的现存组默认展开。显式折叠状态会跨筛选变化保留。
6. 现有 `groupActions()` 先全局过滤，再删除无命中场景；场景 badge 统计唯一动作数。B-4 的“多部位重复计数”不是当前能力。
7. `src/tests/domain/actionGroups.test.ts` 与 `src/tests/integration/libraryFilter.test.tsx` 均硬编码了难度筛选、拉伸二级分组或首部位唯一归属，必须同步改造。

## 设计审查结论

结论：B-4 的方向和现有代码结构兼容，可在 `actionGroups.ts` 的纯函数、`ActionFilterBar.tsx` 的受控输入及 `ActionLibraryScreen.tsx` 的页面状态内完成，不需要数据库迁移、依赖新增或产品 Plan 重开。但口径 3 与口径 9 组合后仍缺一个阻塞定义，见 `HD-1`。

### 已确定边界及建议解法

1. **活动组被收起**：活动场景不因收起而改变，chips 继续归属最后一次“由收起变展开”的场景。只有用户展开另一个场景时才切换活动场景。这样无需维护展开历史栈，且严格符合“最后点开（展开）”。界面须显示当前归属（如“拉伸部位”），避免 chips 控制不可见组时语义不明。
2. **初始活动组**：固定为拉伸，不以搜索后 `groups[0]` 代替。否则搜索令拉伸消失时活动场景会暗中跳转。
3. **切换活动组**：展开另一组时切换活动场景，并把部位选择重置为“全部”；不跨场景保留同名部位，避免新场景不存在该选项或出现隐式过滤。多组仍可同时展开，不做手风琴。
4. **chips 的数据源**：按活动场景的完整动作集合生成，位于搜索和部位筛选之前；只按该场景是否存在规范部位命中决定 `count>0`。搜索、当前所选部位不得让 chips 自己消失或改变顺序。
5. **活动组筛后为 0**：该场景组按现有“空组隐藏”规则从列表消失；chips 保持原活动场景和稳定选项，不随结果为 0 而收缩。若所有场景均无结果，显示现有空结果态。清除筛选须同时清空搜索和部位选择，但活动场景仍保留。
6. **搜索与部位不同时命中**：全局搜索先作用于所有场景；活动场景再与部位条件取 AND，故搜索命中但部位不命中的动作不在活动场景显示；非活动场景只受搜索影响，保持全量部位。活动场景可能因此隐藏，其他搜索命中场景仍显示。
7. **无部位标签动作**：“其他”定义为动作的 `bodypart` 与 `BODY_PART_GROUPS` 无交集，包括 `undefined`、空数组及只有未知/历史脏标签；若同时含至少一个规范部位，则不归“其他”。“全部”包含该场景全部唯一动作，“其他”只包含上述无规范部位动作。
8. **“其他”是否常驻**：不常驻。仅当活动场景完整动作集合中至少有一个“其他”动作时，追加在固定规范部位之后；搜索无命中不会令其消失。
9. **动作编辑/删除后的失效选择**：数据刷新后若当前所选部位不再存在，自动回退“全部”，不得留下无对应 chip 的隐藏状态。
10. **分组与渲染**：三个场景均平铺动作卡片，不再产生或渲染 `ActionBodyPartGroup` / `library-subgroup-*`。场景归属规则及热身优先级不变。
11. **难度范围**：只删除动作库筛选状态、chips 和相关文案；`Action.difficulty`、标签词表、持久化、动作编辑器、种子难度和流程模板难度角标均不动。
12. **自动展开**：名称搜索维持现有“命中组默认展开、显式折叠优先”的行为；部位筛选只针对活动场景，不得把其他折叠组自动展开。若活动场景被用户显式收起，选择 chip 不强行展开。

### 无障碍与触控规范

- 搜索框保留 `accessibilityLabel="搜索动作名称"` 与清晰 hint。
- 若 `HD-1` 选择单选：容器使用 `accessibilityRole="radiogroup"`，每个 chip 使用 `accessibilityRole="radio"` 和 `accessibilityState={{ selected }}`。
- 若 `HD-1` 选择多选：不得伪装成 radio；每个部位项使用 `accessibilityRole="checkbox"` 和 `accessibilityState={{ checked }}`，“全部”作为明确的复位按钮或按最终交互定义实现。
- 容器 label 必须包含活动场景，如“筛选拉伸部位”；chip label 必须包含场景、部位及选中语义所需信息，不只朗读孤立的“全部”。
- 每个可点击 chip 的 `minHeight`、必要时 `minWidth` 均不得小于项目 `MIN_TOUCH_SIZE=48`；保留文字放大上限和可换行布局。
- 场景组头继续使用 `button`、`accessibilityState.expanded`、动作数 label 与展开/收起 hint。

### testID 契约

- 保留：`library-search`、`library-group-{场景}`、`library-group-{场景}-count`、`library-empty`、`library-clear-filters`。
- 新增/替换：chips 容器使用 `library-bodypart-chips-{活动场景}`；chip 使用 `filter-bodypart-{部位}`，其中复位项为 `filter-bodypart-全部`、条件项可为 `filter-bodypart-其他`。同一时刻只渲染一个活动场景的 chips，因此不需要在 chip ID 中重复场景。
- 删除并禁止测试继续依赖：`filter-difficulty-*`、`library-subgroup-*`。
- 集成测试必须同时断言 testID、可访问性 role/state 和 UI 结果，不得只靠文字碰巧存在。

## In Scope

- 重构动作库纯函数：场景内规范部位集合、动态 chip 选项、交集命中、“其他”归属、搜索与活动场景部位筛选组合。
- 将 `ActionFilters` 中的难度筛选移除；按最终 `HD-1` 决策建立最小部位筛选状态。
- 增加并维护 `activeScene`，实现默认拉伸、展开时切换、收起时不切换及切换重置规则。
- 三个场景统一平铺，移除拉伸二级部位标题和旧渲染分支。
- 更新空结果说明和“清除筛选”行为，使其只提搜索/部位。
- 完成可访问性、48dp 触控目标和稳定 testID。
- 改造指定旧测试并补齐本计划 DoD 的单测、集成测试与真机证据。

## Out of Scope

- 不改 PRODUCT_PLAN_V1.2、DEV_BASELINE、数据库 schema 或种子数据。
- 不删除或改写动作难度字段、动作编辑器难度设置、标签持久化。
- 不改流程模板页及 B-3 ④的分组/难度角标。
- 不改动作场景判定优先级，不将多场景标签动作复制到多个一级场景。
- 不引入新依赖、全局状态或可复用筛选框架。
- 不做手风琴；不强制同时只展开一个场景。

## Task Breakdown

| Task ID | Priority | Role | Status | Notes |
|---|---|---|---|---|
| TASK-018-H | P0 | Task Manager / Human | DONE（2026-09-19 HD-1=A） | 完成 `HD-1`，把结论回填派工目标；不修改产品 Plan 版本或 DEV_BASELINE。 |
| TASK-018-B | P0 | Builder | DONE | 读取本计划与 `HD-1` 结论；以现有纯函数和页面局部 state 完成最小改造；同步更新两份旧测试；不得改流程模板页、难度数据或持久化。 |
| TASK-018-CR | P0 | Code Reviewer | DONE | 检查口径逐条映射、搜索/部位作用域、空组与计数语义、未知标签、“其他”、状态失效回退、48dp、role/state/label、testID；确认无范围外改动。 |
| TASK-018-QA | P0 | QA | DONE | 先做真机能力预检；执行定向测试、全量 Jest、typecheck，再在 xagapro / Expo Go 验证三场景切换、折叠、多开、搜索组合、空结果、触控与读屏语义。 |
| TASK-018-S | P0 | Supervisor | DONE | 复检 Builder/Reviewer/QA 证据与 `HD-1` 一致；确认 P0=0、blocking P1=0 后交回 TM。 |

## Risks

- **R1｜规格矛盾**：平铺列表下“动作出现在多个部位下/组头重复计数”可能意味着重复卡片、唯一卡片或多选交集；未完成 `HD-1` 就实现会导致 Reviewer/QA 无统一判据。
- **R2｜动态选项抖动**：若 chips 从搜索后结果生成，输入搜索词会令选中 chip 消失。以场景完整集合生成并对失效选择回退可规避。
- **R3｜活动场景漂移**：若使用过滤后第一个 group 推导活动场景，搜索会隐式改变 chips 归属。活动场景必须使用独立 state。
- **R4｜作用域串扰**：若继续把所有筛选塞进一次全局 `groupActions()`，部位条件容易误伤非活动组。纯函数须显式接收活动场景/部位或分两阶段处理。
- **R5｜React key/操作歧义**：若最终选择重复渲染同一动作卡片，现有 `key={action.id}` 会冲突，且同一编辑/删除入口重复出现；必须在 `HD-1` 中明确接受并定义 key 与 badge 规则。
- **R6｜旧回归假绿/假红**：旧测试硬编码难度 ID、二级分组和唯一首部位规则；仅新增测试而不改旧测试不可验收。

## Human Decisions Needed

### HD-1（阻塞 Builder）：部位 chips 选择模型及平铺后的重复语义

【请你决策】
选择以下一种验收口径：

1. **A｜单选、唯一卡片（建议）**：一次选择“全部”或一个部位；多标签动作选择任一对应部位都能出现，但同一画面只出现一次；场景 badge = 当前实际显示的唯一动作卡片数。“重复计数”理解为该动作可分别计入多个部位的候选集合，不在同一平铺结果重复。
2. **B｜多选、唯一卡片**：允许同时选多个部位，部位间为 OR；动作命中任一已选部位即出现且去重；badge = 当前实际显示的唯一动作卡片数；“全部”清空部位选择。
3. **C｜多选、重复卡片/重复计数**：动作每命中一个已选部位就重复渲染一次，badge 统计命中关系数而非唯一动作数；需接受相同动作卡片和编辑/删除入口重复出现。

【说明】
现口径 3 明说允许重复计数，口径 9 又要求取消部位标题并平铺，无法从文字唯一推导 A/B/C。A 最贴合移动端筛选习惯、现有 radio 交互和“平铺”语义，改动及误操作风险最低。

除 `HD-1` 外无需用户再拍板；本计划“设计审查结论”中的其余规则可直接作为实现与验收默认值。

## DoD

### 单元测试

- `src/tests/domain/actionGroups.test.ts` 必须改造：删除难度筛选、拉伸二级组、`bodyPartOfAction()` 首命中唯一归属等旧断言。
- 覆盖三个场景的动态选项：固定顺序为 全部→颈→肩→胸→背→腰腹→髋臀→腿→小腿→全身→其他，仅输出该场景存在的项；“其他”只在存在无规范部位动作时追加。
- 覆盖多标签交集：动作的任一规范部位命中即可；同时含规范和未知标签不进“其他”；无标签、空标签、未知-only 进“其他”。
- 覆盖搜索 trim/忽略大小写；活动组为搜索 AND 部位，非活动组只受搜索。
- 覆盖场景空结果删除、chips 数据源不随搜索/当前部位收缩、失效选择应回退“全部”所需的纯函数行为。
- 按 `HD-1` 覆盖单选或多选、去重或重复及 badge 计数算法；测试名称必须直接写明最终语义。

### 集成测试

- `src/tests/integration/libraryFilter.test.tsx` 必须改造：删除 `filter-difficulty-*`、`library-subgroup-*` 及“难度过滤后只剩核心”旧断言。
- 默认活动场景为拉伸，默认仅第一组展开；chips 为拉伸实际部位且顺序正确。
- 展开热身后两组同时保持展开，活动场景切为热身、部位选择重置“全部”、chips 集合切换；再展开核心训练同理。
- 收起活动组后 chips 仍归该组；收起非活动组不改变 chips；选择部位不自动展开其他组。
- 部位筛选只改变活动场景的卡片和 badge，其他已展开场景保持完整（仅叠加全局搜索）。
- 全局搜索同时过滤所有场景；活动场景搜索命中但部位不命中时该组隐藏、chip 不消失，其他场景命中仍显示。
- “全部”包含无部位动作；“其他”只显示无规范部位动作；不存在“其他”动作的场景不渲染该 chip。
- 所有场景无命中时显示 `library-empty`；`library-clear-filters` 清空搜索和部位选择并恢复结果。
- 断言难度 chips 和二级部位标题/testID 不再存在；难度数据仍可经动作编辑器读取/修改的既有测试保持通过。
- 断言 `testID` 契约、正确 accessibility role/state/label，以及 chip 样式使用 `MIN_TOUCH_SIZE` 而非 36dp。

### 自动化命令

- `npm test -- --runInBand src/tests/domain/actionGroups.test.ts src/tests/integration/libraryFilter.test.tsx`
- `npm test -- --runInBand`
- `npm run typecheck`
- 三条命令均须 exit 0；不得只凭控制台文字判定。

### 真机验证（xagapro / Expo Go）

- 每个 QA session 先通过设备连接、Expo Go 启动、页面可操作的能力预检；失败则停止正式真机结论并记录阻塞。
- 验证默认拉伸、热身/核心训练切换、多个场景同时展开、活动组收起后归属、切换后重置“全部”。
- 验证每个场景的动态 chips 顺序与条件出现；重点检查热身和核心训练不再平铺前缺失部位筛选。
- 验证多标签动作按 `HD-1` 最终口径命中及 badge；验证“全部/其他”、搜索 AND 部位、非活动组仅受搜索、空结果与清除筛选。
- 验证无难度 chips、无二级部位标题，流程模板页分组和难度角标未受影响。
- 使用 TalkBack/可访问性树至少确认：chips 容器朗读活动场景，选项角色与选中状态正确，场景组头 expanded 状态正确；实际点按目标不小于 48dp、文字放大后不截断关键标签。
- 本任务无音频，不执行音频听测或拔线流程。

### 必须同步改造的旧测试文件

1. `src/tests/domain/actionGroups.test.ts`
2. `src/tests/integration/libraryFilter.test.tsx`

上述两份缺一不可；同时要求全量 Jest 与 typecheck 无新增红灯。

## B-3 已验收 Requirement / DoD 变更影响

本任务只在本 Task Plan 记录应替换文案，不直接修改已验收产品 Plan、HANDOFF 或其他文档。

1. **B-3 ① 原文**：“动作库按场景折叠分组（拉伸/热身/核心训练，默认只展第一组，组头数量badge）。”  
   **改为**：“动作库按场景独立折叠分组（拉伸/热身/核心训练，默认只展开拉伸，可多组同时展开）；最后一次由收起变展开的组为活动场景，组头 badge 按 `HD-1` 最终口径统计。”
2. **B-3 ② 原文**：“拉伸组内按部位二级分组（颈肩胸背腰腹髋臀腿小腿全身）。”  
   **改为**：“三个场景内部均平铺动作，不显示二级部位标题；顶部为活动场景生成动态部位 chips，规范顺序为颈、肩、胸、背、腰腹、髋臀、腿、小腿、全身，必要时末尾追加其他。”
3. **B-3 ③ 原文**：“顶部筛选chips（难度低中高）+搜索框（按名过滤），筛选后只显命中分组。”  
   **改为**：“顶部保留全局名称搜索，彻底移除动作库难度 chips；新增活动场景动态部位 chips。部位只过滤活动场景，其他场景只受全局搜索；活动场景内搜索与部位取 AND；无命中场景隐藏，chips 集合不随搜索结果收缩。”
4. **B-3 ④**：不修改。流程模板页继续按场景分组并保留既有难度角标实现。
5. **B-3 ⑤ 原文**：“分组/筛选/搜索单测+集成测试+真机验证。”  
   **改为**：“动态 chips 生成（顺序/空项剔除/其他条件出现）、多标签交集、活动场景切换与收起、非活动组不受部位影响、全局搜索组合、空结果/清除、可访问性与 48dp 触控目标均有单测/集成测试；同步改造 `actionGroups.test.ts`、`libraryFilter.test.tsx`；xagapro / Expo Go 真机验证通过。”

