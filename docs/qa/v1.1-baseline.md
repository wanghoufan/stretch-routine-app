# V1.1 Hardening 基线记录（R002 / R003）

- 记录人：builder（TASK-007 Wave1；按派工明确指令落 `docs/qa/`，属「builder 技术基线证据」而非 qa 评审结论——同类归属冲突沿用挂账 TM 裁定的既有做法）
- 记录时间：2026-09-18
- 工作分支：`hardening/v1.1`（自 `main@bc1f39f` 切出）
- 基线 tag：`v1.0-implemented-bc1f39f` → `bc1f39f041cfa4a71696534ed86aee25c44d09d1`
- DEV_BASELINE：`PRODUCT_PLAN_V1.2`
- 环境：macOS / Node v24.19.0 / npm 11.17.0

> 本文件只记录 **本轮实测原始结果**，不引用历史记录当通过证据（PRODUCT_PLAN_V1.2 Key Assumptions）。

## R002 重跑基线（真实结果）

| 命令 | 结果 | 备注 |
|---|---|---|
| `npm ci` | PASS（exit 0） | `added 773 packages, and audited 774 packages in 13s`；`10 moderate severity vulnerabilities`（npm audit 报告，未执行 `audit fix`，怕动 lockfile） |
| `npx tsc --noEmit` | PASS（0 error，无输出） | 与 V1 历史声明一致 |
| `npx jest --runInBand` | PASS | `Test Suites: 23 passed, 23 total` / `Tests: 154 passed, 154 total` / 5.251s |
| `npx expo-doctor` | **20/21 通过，1 项 FAIL** | 见下「已知基线问题-1」 |

### 已知基线问题-1：expo-doctor app.json schema（非本轮引入，未修）

```
✖ Check Expo config (app.json/ app.config.js) schema
Errors validating fields in app.json:
  should NOT have additional property 'newArchEnabled'.
  should NOT have additional property 'splash'.
1 check failed, indicating possible issues with the project.
```

- 事实核对：`npx expo config --type public` 能正常解析全部字段（`sdkVersion: 57.0.0`），JS bundle 不受影响。
- 影响面：仅 config schema 校验；`newArchEnabled` 在 SDK 57 已是默认，`splash` 已迁到 `expo-splash-screen` 插件配置。
- 处理：**本轮不改**。改 `app.json` 属构建/原生配置面，且 V1.1 的 native Exit Gate（R004–R006）本轮不执行；留给 native/构建通道轮次统一处理并复测 expo-doctor，避免 Wave1 手改配置影响后续 CNG。
- 挂账：TM 裁定归属与时机。

## R003 Build / Version Baseline（app.json + package.json 实测核对）

| 项 | 实测值 | 来源 | 判定 |
|---|---|---|---|
| Expo SDK | `~57.0.23`（安装 57.0.23） | `package.json` / `node_modules/expo/package.json` | 与 PLAN「Expo SDK 57」一致 |
| React Native | `0.86.3` | `package.json` | 与 PLAN「RN 0.86.3」一致 |
| React | `19.2.3` | `package.json` | — |
| expo-sqlite / expo-speech / expo-status-bar | `~57.0.3` / `~57.0.3` / `~57.0.1` | `package.json` | — |
| TS | `~6.0.3` | `package.json` | — |
| app version（versionName 源） | `1.1.0` | `app.json` `expo.version` | 与 PLAN「package/app version 统一到 V1.1」方向一致 |
| versionCode | `2` | `app.json` `expo.android.versionCode` | 较 V1 `1.0.0/code 1` 提升，符合 R039 方向 |
| package | `com.stretchroutine.v1` | `app.json` `expo.android.package` | 与真机已装包名一致 |
| compileSdkVersion | `36`（未在 app.json 覆写，取默认） | `expo-modules-core` `ExpoModulesCorePlugin.gradle:65` 默认 36；RN 0.86.3 `gradle/libs.versions.toml` compileSdk=36 | 与 PLAN「compile/target SDK 36」一致 |
| targetSdkVersion | `36`（默认） | 同上 `:69` 默认 36；RN 0.86.3 targetSdk=36 | 与 PLAN 一致 |
| minSdkVersion | `24`（默认） | 同上 `:68` 默认 24；RN 0.86.3 minSdk=24 | **PLAN 未定调**；minSdk smoke/提升决策属 R043/R044，本轮只记录，不擅改 |
| newArchEnabled | `true` | `app.json` | SDK 57 默认新架构；expo-doctor 报 schema 见上 |
| android versionName | 无独立字段 | app.json 只有 `expo.version` | Android versionName 由 `expo.version` 派生，无单独声明 |

### 版本治理口径（本轮记录，不自行改）

- `package.json#version = 1.0.0` 与 `app.json#expo.version = 1.1.0` **当前不一致**。
- 该统一属 **R039**（统一 package/app version、versionCode、tag、HANDOFF），由后续轮次/构建通道执行，本轮 Wave1 不动，避免与 native 打包版本线冲突。
- 本轮新增分支/tag 只服务纯 JS hardening 工作线，不构成 release 版本发布。

## 本轮 Wave1 范围声明（诚实边界）

- 只做纯 JS hardening：R001、R002、R003（记录）、R007–R021。
- R004–R006（native module / CNG / elapsed-boot-exit reason 真机验证）、R022–R034（FGS / 通知 / Doze / 矩阵）**不在本轮**，留待构建通道；本轮**不手写 `android/` 目录、不伪造 native 已验证**。
- `MonotonicClock` 在 Expo Go 环境用 `Date.now()` 实现，代码内已标注待 native `elapsedRealtime` 替换；`bootCount` 用「启动时 Date.now() + 内存标记」近似，代码内已标注待 native `getBootCount()` 替换。两者均已通过可 mock 的端口隔离，替换时不动业务逻辑。

## Wave1 完工验证（2026-09-18，全部实测）

| 命令 | 结果 | 备注 |
|---|---|---|
| `npx tsc --noEmit` | PASS（exit 0，0 输出行） | 重构后无类型错误 |
| `npx jest --runInBand` | PASS（exit 0） | `Test Suites: 28 passed, 28 total` / `Tests: 182 passed, 182 total`（基线 154 → 新增 28） |
| `npx expo export --platform android` | PASS | `Android Bundled 4186ms index.ts (699 modules)`，产物 `.hbc` 1.8MB（release 模式） |
| `npx expo start --port 8123` + `curl /index.bundle` | HTTP 200 / `Android Bundled 1916ms index.ts (868 modules)` | 开发模式 bundle 4,751,194 bytes；`node --check` 通过（`BUNDLE_PARSE_OK`）；8123 为临时空闲端口（8082 被占用，未动他人服务），验证后已释放并 kill 进程 |

- 新增测试文件：`migrationsV2.test.ts`、`startRoutineService.test.ts`、`startConflict.test.tsx`、`activeSessionBanner.test.tsx`、`snapshotImmutability.test.tsx`；扩写 `sessionRecovery.test.ts`、`sessionRepository.test.ts`、`runnerMachine.test.ts`。
- R013 覆盖项与落点：wall ±1h/±1d 不影响倒计时 → `sessionRecovery.test.ts`「ignores wall-clock jumps」；monotonic → `runnerTime/runnerMachine` 全套；boot mismatch → recovery + startRoutineService；corrupt JSON/未知版本 → `sessionRepository.test.ts`；V1→V2 数据保留 + 旧 session 清理 → `migrationsV2.test.ts`；snapshot 不可变 → `snapshotImmutability.test.tsx`。
- 诚实边界：以上只证明纯 JS 层。FGS / native elapsedRealtime / bootCount / exit reason / 真机息屏与重启矩阵 **未验证**，属 R004–R006 与 R022–R034，留待构建通道；本轮未创建 `android/` 目录、未手写 native。
- R035（条件 P0 WAL）：本轮新增了 schema migration，但**未改**数据库连接/初始化配置，也未做任何 WAL 声明；是否触发由 TM 裁定。
- 全量 expo-doctor 未复跑（app.json schema 问题同上「已知基线问题-1」，属 native/构建面，本轮不修）。

