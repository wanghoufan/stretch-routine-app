# 经验一句话

> 收工时编排者派经验记录追加一句“下次别踩”，下次开工先读。只追加，不搞事件/升级。

- 2026-09-08：模型不够就改 USER_MODEL_OVERRIDE.md 切备用模型，精确ID，禁 `gpt-5.6` 别名。换谁、用到几时，你定。
- 2026-09-08：老项目迁移业务原地不动，能跑优先，搬家broken就留原地记映射。
- 2026-09-08（雷达副本实战）：迁移默认装依赖跑基线，lint FAIL不卡dev启动（先起服务验页面，lint并行修）；有UI必computer-use真机目检（首页+详情页，点链进详情别跟地址栏较劲）；资料包/验收原件先确认在不在，不在就按HANDOFF替代清单核对并记账。
- 2026-09-08（Terra真调）：`codex exec -m gpt-5.6-terra -s read-only --skip-git-repo-check "任务" </dev/null` 即通；两坑：非信任目录必加 `--skip-git-repo-check`，stdin必重定向不然hang；复核员角色实测可用，全自动零弹窗。
- 2026-09-09（外部审查）：3P0+9P1+10缺失全修完；教训：模板与卡禁止各写一套字段（账本schema只认AGENTS），校验命令必须真跑一遍再定稿，空目录/残留模板分发前清掉。
- 2026-09-09（缓存）：同链续session、静态打头动态押后、长了就压；各家通用，不追99%，用完别急着关。
- 2026-09-10（升级口径双A）：升级只认supervisor累计打回2次自动升、senior再被打回2次即停线找人；账本schema锁死枚举，定稿前必跑坏例校验。
- 2026-09-10（复验）：坏例校验必须看 exit 码，不看打印——只计算不 exit(1) 的校验等于没验；结论只许落在 review/qa/审查报告，HANDOFF 只记状态。
- 2026-09-12（两阶段整改）：模型与治理分批验（A门禁过才进B），版本号用户定才升（本次2.2转正），账本冻结期实绩只记HANDOFF存档。
- 2026-09-13（迁移提示词）：新建文件前先看已有文件的职责，别造重复件（"自举"与"迁移整理"重复，用户发现后合并为一份）；README 归位清单要"文件＋落位路径"成对写，只列文件不写位置会让提示词里的引用悬空。
- 2026-09-15（sop与软链）：基础设施规范用短名（docker/supabase/sqlite）去版本号引用，正文互引同步去版本；分工表软链制，真源只在母版，改母版即全项目同步，禁拷实文件。
- 2026-09-18（HyperOS真机取证）：HyperOS的adb安装系统确认框10秒不点按按拒绝处理（需uiautomator点继续安装）、eas免费队列约48分钟、用户禁音红线下只做安装+冷启动截图+logcat静默取证、语音实测挂账明天。
- 2026-09-18（本地打包+HyperOS装机）：免sudo装JDK/SDK到用户目录即可本地出包；HyperOS的USB安装确认必须等倒计时走完再点继续安装，点早直接判拒绝；USB线连着手机无声是常态，听测先拔线。
- 2026-09-19（Metro 陈旧 bundle）：真机验收前先取设备实际请求的 bundle（manifest 的 `launchAsset.url`）核本轮改动标记（新增文案/常量），不含就停 Metro 用 `--clear` 重启并全部重跑——Metro 会让该 URL 命中陈旧缓存键，force-stop/start 甚至 touch 源文件都刷不掉，不核就是拿旧包验出假通过/假失败。
- 2026-09-19（全绿≠达标）：测试全绿只证明「已写的断言」通过，收工前必须拿 Plan DoD 逐条对账——本轮「界面可见归属未落地」「两条集成断言缺失」都是全绿状态下被 reviewer/QA 抓出来的；DoD 每一条都要能落到一条会红的断言或一次真机取证。
- 2026-09-19（口径先消歧）：交互需求里「平铺/多选/重复计数/去重」这类词必须在开工前收敛成唯一口径（本轮 HD-1=A：单选+同屏去重+badge=唯一卡片数），否则冲突条款拖到 planner 复核才引爆，Builder 按错口径白写一轮。
- 2026-09-24（真机无声四连坑）：扬声器媒体音量0＋第三方sherpa引擎无中文包＋投屏/录屏劫持音频到remote_submix＋重启断adb reverse，逐项adb取证（dumpsys audio/media_projection/logcat AudioTrack），修完以release包离线验收，亲耳听见为准。
