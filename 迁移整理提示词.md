# 迁移整理提示词｜老项目一句话归位（含自举取包）

> **本机专用**：源路径已写死（生成于 2026-09-13），换机器/换目录时改下面第 0 步里那一行。

```text
【迁移整理】

你是整理工，不改业务逻辑。全程自动，中途不要问我、不要给我看 diff，跑完只汇报一次。

0. 取包：去本机治理仓库把模板包取来铺到本项目。这一步每次都做，不要因为本项目"看起来已经有 AGENTS.md / docs 了"就跳过——老项目本来就有自己的治理文件，跳过就等于永远用不上新模板包。本项目已有同名文件属正常，照下面冲突处理办；重复执行安全（一样的会跳过）。
   源路径（整条原样照抄，含中文、全角括号、竖线和空格，命令里必须用引号把整条路径包住）：
   /Users/zzymima0000/Developer/coding/4.Templates（PC）/2026-09-09 丨 MAC 丨 ORCA V2.1 治理模板 丨 分发版-2026-09-11/老项目迁移模板包
   把源包内容铺到本项目根目录（不要 .DS_Store，不要多套一层文件夹）：AGENTS.md、GOVERNANCE_VERSION、docs/（roles、pm、handoff、model、qa、review、sop 全要，其中 sop 含 docker.md、supabase.md、sqlite.md、android.md、webqa.md、decision-router.md）、scripts/（orchestration、decision 全要）、迁移整理提示词.md、编排者提示词.md、外部开发者提示词.md、归位表.template.md；另把 Orca 通用编排者持续推进协议.md、Orca 编排治理监督者提示词.md 放到本项目 docs/prompts/，把归位表.template.md 放到本项目 docs/templates/。USER_MODEL_OVERRIDE.md 不拷贝：已有实文件先按铁律备份再删，然后建软链指母版真源（目标＝第 0 步源路径去尾段包名＋/USER_MODEL_OVERRIDE.md），改母版即全项目同步；跨机器断链时拷实文件并记 HANDOFF 一笔。
   冲突处理（铁律：不删、不覆盖，撞了改名留旁边）：本项目没有的→直接放；已有且与模板一模一样→跳过；已有但不一样→把原文件改名成「原名.旧版-2026-09-13」留在同级（不删），再放模板版，并记一笔。只有 AGENTS.md 特殊：原版改名备份后，把它里面本项目专属的规矩原样附到新版 AGENTS.md 末尾，加一节「本项目原有规则（迁移自旧 AGENTS.md）」，不丢规矩。不属于上面清单的原有文件（本项目自己的文档、src、配置、README）→一律不碰。

1. 按模板目录结构把当前项目文件分类归位：该进docs的进docs；业务原地不动。
2. 铁律：搬了会broken的一律不搬，留原地+记一条映射（原路径→治理引用路径）。
3. 基线（默认干，不用问人）：装依赖（pnpm/npm install）→跑 lint/ts-check/test→记 PASS/FAIL。lint挂了边修边继续，不拿lint卡dev启动；node_modules缺失就装，不要问。
4. 有UI的项目：有 UI 必目检（首页+一个详情页），无 computer-use 条件时 curl＋截图替代并记账；dev起后台+curl验状态码，截图路径记下来。
5. 输出《项目文件归位表》：已归位/留原地+原因/备份清单/引用路径，落盘到本项目 `docs/templates/归位表.md`（覆盖旧版，git可回溯）。留原地不算失败。
5.5 建本项目 `docs/handoff/HANDOFF.md` 首版（照包内 HANDOFF.template.md，有旧版则按第 0 步冲突铁律先备份）：PROJECT_PHASE 填现状（未开工填空）、映射与备份清单记执行链、基线结果记剩 P0/下一步、CHANGE_REQUEST 填 NONE。不断会话可不看它；断会话/换人靠它接续。
5.6 删账本示例行：`docs/model/` 下两表示例行在首个真实任务（TASK）/首个真实派工（DISPATCH）前删除（各一行 `_example`，删后 HANDOFF 记一笔）。
6. 写 GOVERNANCE_VERSION（内容：版本以本仓库Git历史为准），完事按《编排者提示词》开工。

末尾一次性汇报：放了几个、备份了几个、跳过了几个、测试结果、没验什么+原因。
```
